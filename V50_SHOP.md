# V50 — Treasure & Special Shop

- دکمه «🎁 گنجینه» دیگر کیف پول را نشان نمی‌دهد و فروشگاه صندوق را باز می‌کند.
- `/chests` و `/فروشگاهصندوق` فروش صندوق با سکه را نشان می‌دهند.
- `/specialshop` فروشگاه ویژه با قیمت تومان را نشان می‌دهد.
- `/ordershop <product_id>` یک سفارش در وضعیت `pending` ثبت می‌کند.
- وب‌اپ APIهای `/api/real-shop` و `/api/real-shop/order` را دارد.
- محصولات پولی ثابت و غیرتصادفی هستند؛ صندوق پولی شانسی اضافه نشده است.
- برای دریافت پول واقعی باید یک درگاه تأییدشده و callback/verify امن به سرور متصل شود؛ این نسخه بدون credential درگاه، پولی از کاربر دریافت نمی‌کند و سفارش را خودکار موفق اعلام نمی‌کند.
