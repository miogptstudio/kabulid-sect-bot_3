# Character system — V40

- استخر فعال کاراکترها دقیقاً ۳۰ نام دارد.
- هر یک از ۳۰ کاراکتر تصویر اختصاصی محلی دارد: `assets/characters/01.jpg` تا `30.jpg`.
- تصاویر از سیستم عمومی DiceBear برای ۳۰ کاراکتر فعال استفاده نمی‌کنند.
- `/mychars` و `/charinfo` برای هر کارت از تصویر همان کاراکتر استفاده می‌کنند.
- داده‌های `chars_owned` و سایر namespaceهای پایدار حذف یا reset نمی‌شوند.
- کاهش استخر به ۳۰ فقط روی قرعه‌کشی‌های جدید اثر دارد و داده‌های قبلی بازیکنان دست‌کاری نمی‌شود.
