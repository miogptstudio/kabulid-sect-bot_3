# Image panel mapping

Replace each placeholder URL in `bot/panel_images.py` with a real Telegram
`file_id` (recommended) or a stable CDN URL.

Panels: help, profile(male/female), duel, cultivation, servants(male/female),
marriage, sect, kingdom, arena, ranking, market, shop, jobs.

## V18
هر بخش تصویر مستقل و متفاوت خود را از `assets/panels` دریافت می‌کند. تصویر اصلی کاتالوگ در `assets/panel_catalog_v18.jpg` قرار دارد.
