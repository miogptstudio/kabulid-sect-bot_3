# V51 — پرداخت دستی + تأیید خرید توسط ادمین

## جریان خرید
1. بازیکن `/specialshop` یا «فروشگاه ویژه» را باز می‌کند.
2. با `/ordershop PRODUCT_ID` سفارش می‌سازد.
3. شماره کارت مقصد از متغیر محیطی `PAYMENT_CARD_NUMBER` خوانده می‌شود.
4. بازیکن مبلغ را خارج از ربات واریز می‌کند.
5. با `/payreceipt ORDER_ID` وارد حالت ارسال رسید می‌شود.
6. عکس رسید را می‌فرستد.
7. رسید همراه دکمه‌های «تأیید خرید» و «رد رسید» برای ادمین‌ها ارسال می‌شود.
8. فقط `ADMIN_IDS` می‌توانند تأیید/رد کنند.
9. تأیید خرید فقط یک بار جایزه را اعمال می‌کند و سفارش `approved` می‌شود.

## پنل ادمین
- `/purchaseorders` — نمایش سفارش‌های دارای رسید و در انتظار بررسی.
- در پیام رسید، دکمه‌های تأیید و رد قرار دارند.
- در وب‌اپ نیز بخش «خریدهای بازیکنان» در پنل مدیریت اضافه شده است.

## تنظیم شماره کارت
شماره کارت را داخل کد یا چت قرار ندهید. روی سرور/Render در Environment Variables مقدار زیر را تنظیم کنید:

`PAYMENT_CARD_NUMBER=....`

در `.env.example` فقط نمونهٔ جایگزین قرار داده شده است.

## نکته امنیتی
ربات تأیید بانکی خودکار انجام نمی‌دهد؛ ادمین باید رسید را با اطلاعات حساب مقصد تطبیق دهد و سپس تأیید کند. برای پرداخت خودکار، درگاه رسمی و callback/verify سمت سرور لازم است.
