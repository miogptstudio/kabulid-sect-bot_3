# V49 — bugfixes

- تذهیب دوگانه: cooldown از ۲۴ ساعت به ۳۰ دقیقه تغییر کرد.
- نمایش زمان باقی‌مانده برای cooldownهای کمتر از یک ساعت به دقیقه اصلاح شد.
- state نامرتبط `is_virgin` از اجرای تذهیب دوگانه حذف شد؛ تذهیب دوگانه نباید وضعیت شخصی نامرتبط را تغییر دهد.
- تمام فایل‌های Python با `compileall` بررسی شدند و خطای Syntax/AST پیدا نشد.
- `webapp/app.js` با Node syntax check شد.

> تست کامل runtime در محیط ساخت قابل اجرا نیست چون dependencyهای Python در این محیط نصب نیستند و دسترسی شبکه برای pip وجود ندارد. فایل requirements برای استقرار دست‌نخورده باقی مانده است.
