# v53 — رفع باگ‌های نسخه جدید

- جلوگیری از اعمال دوباره جایزه خرید در تأیید همزمان ادمین‌ها با قفل فرآیندی و marker `fulfilled_at`.
- پرداخت دستی فقط وقتی فعال اعلام می‌شود که `PAYMENT_CARD_NUMBER` دقیقاً یک شماره کارت ۱۶ رقمی معتبر داشته باشد.
- سفارش وب پس از ثبت، مبلغ، شماره کارت و دستور ارسال رسید را برمی‌گرداند تا جریان خرید ناقص نباشد.
- نمایش رسید در پنل وب از طریق proxy احراز‌شده ادمین انجام می‌شود و Bot Token به مرورگر داده نمی‌شود.
- تأیید خرید در وب قبل از اعمال جایزه تأییدیه می‌گیرد.
- ارسال دوباره رسید، یادداشت و وضعیت سفارش بهتر مدیریت می‌شود.
- نسخه وب/ربات به 53.0.0 / 9.0.0 ارتقا یافت.
- تمام فایل‌های Python با AST/compileall و اسکریپت‌های inline وب با `node --check` بررسی شدند.
