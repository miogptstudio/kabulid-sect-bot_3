# 📖 راهنمای کامل Kabulid Sect Bot

این فایل راهنمای نسخه فعلی ربات است. هدف آن این است که کاربر جدید بدون نیاز
به پرسیدن سؤال‌های اضافی بتواند سیستم‌های اصلی ربات را بشناسد.

## 🚀 شروع کار
- `/start` — ساخت/فعال‌سازی پروفایل و نمایش منوی اصلی.
- `/help` — نمایش راهنما و دسته‌بندی دستورات.
- `/commands` — نمایش فهرست دستورات مهم.

> اگر یک دستور پارامتر می‌خواهد، مقدار نمونه را دقیقاً مطابق فرمت همان دستور وارد کنید.

---

## 👤 پروفایل و اطلاعات بازیکن
- `/profile` — نمایش اطلاعات شخصیت، منابع، سطح و وضعیت کلی.
- `/stats` — نمایش آمار و قدرت‌های اصلی.
- `/balance` — نمایش موجودی ارزها.
- `/inventory` — نمایش موجودی آیتم‌ها.
- `/rank` — نمایش رتبه/جایگاه بازیکن در سیستم رتبه‌بندی.

---

## 💰 منابع و اقتصاد
ربات ممکن است چند نوع ارز و منبع داشته باشد. مقدار و نوع ارز موردنیاز هر
آیتم در خود بازار نمایش داده می‌شود.

- قبل از خرید، موجودی خود را بررسی کنید.
- قیمت‌های بسیار بزرگ نیز پشتیبانی می‌شوند.
- اگر خرید ناموفق بود، موجودی، فعال بودن آیتم و شرایط خرید را بررسی کنید.

---

## 🏪 بازار و مغازه
- `/shop` — باز کردن مغازه.
- `/market` — باز کردن بازار در نسخه‌هایی که این دستور فعال است.
- از دکمه‌های داخل مغازه برای مشاهده دسته‌بندی‌ها و خرید استفاده کنید.
- هر آیتم می‌تواند قیمت، موجودی، توضیح و اثر مخصوص خود را داشته باشد.
- آیتم‌های خاص ممکن است شرایط خرید یا ارز متفاوت داشته باشند.

### خطاهای رایج بازار
1. موجودی کافی نیست.
2. آیتم غیرفعال یا تمام شده است.
3. پارامتر واردشده معتبر نیست.
4. اتصال دیتابیس یا migration هنوز انجام نشده است.

---

## 💼 شغل
سیستم شغل برای کسب درآمد/منابع و پیشرفت اقتصادی استفاده می‌شود.

دستورات رایج:
- `/jobs` — نمایش شغل‌های موجود.
- `/job` — نمایش وضعیت شغل فعلی.
- `/work` — انجام کار و دریافت پاداش در صورت آماده بودن شرایط.
- `/myjob` — نمایش شغل فعلی و اطلاعات مربوط به آن.
- `/changejob` — تغییر شغل در صورت فعال بودن امکان تغییر.

> محدودیت زمانی، سطح، انرژی یا شرایط هر شغل را از پیام خود ربات بررسی کنید.

---

## 🧑‍🤝‍🧑 خدمتکاران
خدمتکارها بخشی از سیستم پیشرفت و تذهیب هستند.

- `/servants` یا `/myservants` — نمایش خدمتکارهای شما.
- از شماره خدمتکار برای انتخاب او در دستورهای مربوط استفاده کنید.
- اطلاعاتی مانند تبار، وفاداری، وضعیت ازدواج و پیشرفت در لیست نمایش داده می‌شود.

### ❤️ وفاداری
- `/loyalty شماره` — مشاهده وضعیت وفاداری خدمتکار.
- `/feedloyalty شماره` — افزایش وفاداری در صورت داشتن شرایط لازم.

نمونه:
`/feedloyalty 1`

---

## 💍 ازدواج با خدمتکار
در صورت فعال بودن شرایط ازدواج:
- خدمتکار موردنظر را با شماره انتخاب کنید.
- دستور ازدواج را مطابق راهنمای داخل ربات اجرا کنید.
- وضعیت همسر در اطلاعات خدمتکار ذخیره می‌شود.

نمونه رایج:
`/marry servant 1`

ازدواج باید در دیتابیس ذخیره شود تا بعد از Restart نیز باقی بماند.

---

## ✨ خدمتکارهای تبار الهی
نسخه فعلی شامل خدمتکارهای قدرتمندتر با تبارهای بالاتر است، از جمله:
- الهه‌تبار
- خدای‌تبار
- آسمانی‌تبار
- تبار الهی

این خدمتکارها معمولاً ارزش، قدرت و پاداش بالاتری دارند و ممکن است برای
خرید/استخدام شرایط سخت‌تری داشته باشند.

نمونه نام‌های اضافه‌شده:
- آریانا
- یوناس
- سولارا
- ایلیوس
- نِریا
- کایروس
- آسترا
- اورین

---

## ⚔️ مبارزه و قدرت
بخش مبارزه ممکن است شامل قدرت نبرد، سرعت، دفاع، نفوذ و اثر آیتم‌ها باشد.

- `/duel` — ورود به سیستم دوئل در صورت فعال بودن.
- `/fight` — مبارزه در نسخه‌هایی که این دستور فعال است.
- `/rank` — مشاهده جایگاه رتبه‌ای.

آیتم‌های دفاعی و تهاجمی می‌توانند روی نتیجه مبارزه اثر بگذارند.

---

## 🛡️ آیتم‌های دفاعی
برخی نمونه‌های بازار:
- سپر چوبی — دفاع پایه
- سپر آهنی — دفاع متوسط
- سپر روحی — دفاع و جذب بخشی از آسیب
- سپر انعکاسی — دارای اثر انعکاس
- سپر بهشتی — مقاومت بالاتر در برابر نفوذ
- سپر اژدها — آیتم سطح بالاتر

توضیحات دقیق هر آیتم را هنگام مشاهده آن در مغازه بخوانید.

---

## 📚 آیتم‌های ویژه و تکنیک‌ها
برخی آیتم‌ها ممکن است اثرهایی مثل:
- افزایش قدرت نبرد
- افزایش سرعت
- افزایش دفاع
- افزایش دانش
- یادگیری تکنیک
- افزایش نفوذ
داشته باشند.

اثر دقیق در مشخصات همان آیتم نمایش داده می‌شود.

---

## 🔧 دستورات مدیریتی
این دستورات فقط برای ادمین/مالک ربات در صورت داشتن دسترسی مناسب هستند.
از دستورات مدیریتی برای تغییر داده‌های اقتصادی، مدیریت مغازه، کاربران،
خدمتکارها و تنظیمات استفاده می‌شود.

> نام و دسترسی دستورات مدیریتی ممکن است با تنظیمات نسخه/ادمین متفاوت باشد.

---

## 🆘 وقتی ربات می‌گوید «خطا در اجرا»
این مراحل را بررسی کنید:
1. دستور را دقیقاً مطابق `/help` وارد کنید.
2. اگر دستور شماره می‌خواهد، فقط شماره معتبر وارد کنید.
3. اگر دستور خرید است، موجودی و شرایط خرید را بررسی کنید.
4. اگر دکمه‌ای از منوی قبلی استفاده می‌کنید، دوباره `/start` یا منوی اصلی را باز کنید.
5. اگر خطا ادامه داشت، متن کامل خطا و نام دستور را برای ادمین ارسال کنید.

### خطاهای Telegram
اگر خطایی مثل `can't parse entities` مشاهده شد، معمولاً مربوط به
فرمت‌بندی HTML/Markdown پیام است و باید توسط سازنده ربات بررسی شود.

### خطاهای دیتابیس
اگر خطاهایی مثل `DataError`، `DBAPIError` یا `asyncpg` مشاهده شد،
مشکل معمولاً از نوع داده، migration یا داده نامعتبر در دیتابیس است.

---

## 🧭 پیشنهاد برای کاربر تازه‌وارد
ترتیب پیشنهادی:
1. `/start`
2. `/profile`
3. `/balance`
4. `/inventory`
5. `/jobs`
6. `/work`
7. `/shop`
8. `/myservants`
9. بررسی وفاداری و شرایط خدمتکارها
10. ارتقای تجهیزات و قدرت مبارزه

---

## 📌 نکته مهم
دکمه‌های داخل ربات ممکن است بسته به نسخه، سطح بازیکن، فعال بودن سیستم،
موجودی و دسترسی کاربر متفاوت باشند. همیشه متن همان پیام و `/help` را
مرجع اصلی دستورهای فعال قرار دهید.

## 🧘 قوانین پایه تذهیب
دستور `/cultivationrules` قوانین اولیه تذهیب را یک‌جا نشان می‌دهد. هر قلمرو ۱۰ مرحله دارد و قلمروهای بالاتر انرژی بسیار بیشتری می‌خواهند. ریشه معنوی، تکنیک، نژاد، مسیر تذهیب و بعضی سیستم‌های دیگر روی بازده اثر دارند.

## 💸 انتقال ارزهای رده‌بالا
`/pay` و `/payall` اکنون از همه ارزهای کیف پول پشتیبانی می‌کنند: سکه، روحی، بهشتی، آسمانی، خدا، هرج‌ومرج، پوچی، ازلی، تقدیر، جاودان، خلقت، مطلق، ایمان، سکه اژدها و کارما. مقادیر بزرگ با BIGINT ذخیره می‌شوند.

نمونه:
`/pay 123456789 god 2`
`/payall 123456789 heavenly 5 god 2 void 1 origin 1`

## 🛡 قدرت مستقیم توسط سازنده
سازنده می‌تواند قدرت مستقیم نمایش‌داده‌شده در پروفایل را به بازیکن بدهد:
`/givepower TELEGRAM_ID مقدار total`
یا روی پیام بازیکن ریپلای کند و بنویسد:
`/givepower مقدار total`
این قدرت در پروفایل و محاسبه قدرت رزمی باقی می‌ماند.

## 👑 انتقال تذهیب سطح بالا
سازنده می‌تواند کل وضعیت تذهیب یک بازیکن، حتی قلمروهای بسیار بالا، را به بازیکن دیگر کپی کند:
`/transfercult SOURCE_TELEGRAM_ID TARGET_TELEGRAM_ID`
یا روی پیام مقصد ریپلای کرده و بزند:
`/transfercult SOURCE_TELEGRAM_ID`
قلمرو، مرحله، انرژی، استعداد، ریشه معنوی و نوع بدن منتقل می‌شوند.

# 🌟 سیستم‌های توسعه‌یافته جدید

## 🧬 تبار
- `/bloodlines` — فهرست تمام تبارها و ضریب آن‌ها.
- `/mybloodline` — نمایش تبار فعلی.
- `/setbloodline TELEGRAM_ID تبار` — مخصوص سازنده.

## ⚔️ قدرت رزمی کامل
- `/powerstats` — نمایش قدرت نهایی و اجزای تشکیل‌دهنده آن.
- قدرت می‌تواند از تذهیب، سلاح، زره، بدن، روح، تکنیک‌ها، تبار و پاداش مستقیم ادمین تشکیل شود.

## 🌳 خانواده و خاندان
- `/familytree` — نمایش روابط خانوادگی ثبت‌شده.
- `/mychildren` — نمایش فرزندان در سیستم خانواده.

## 👑 حکومت و قلمرو
- `/kingdom` — نمایش حکومت بازیکن.
- `/setkingdom TELEGRAM_ID نام` — تنظیم نام حکومت، مخصوص سازنده.
- `/kingdomcity TELEGRAM_ID نام شهر` — افزودن شهر به حکومت، مخصوص سازنده.
- `/setkingdomstat TELEGRAM_ID treasury|army|population|tax مقدار` — تنظیم آمار حکومت، مخصوص سازنده.

## 🧘 قوانین پایه تذهیب
- `/cultivationrules` — راهنمای پایه و منطق سیستم تذهیب.


# 🌌 رشد ویژگی‌ها با بالا رفتن قلمرو

از این نسخه، قلمرو تذهیب فقط یک عنوان نیست و روی ویژگی‌های مشتق‌شده شخصیت
اثر می‌گذارد. با افزایش قلمرو و مرحله:

- ⚔️ قدرت رزمی افزایش پیدا می‌کند.
- ⚡ سرعت افزایش پیدا می‌کند.
- ❤️ طول عمر پایه افزایش پیدا می‌کند.
- ⏳ عمر باقیمانده بر اساس سن فعلی محاسبه می‌شود.
- 🩹 سرعت بازیابی/ریکاوری افزایش پیدا می‌کند.
- 👁 ادراک نیز افزایش پیدا می‌کند.

مرحله‌های داخل یک قلمرو هم به‌تدریج این مزایا را بیشتر می‌کنند.

## پیشنهاد نمایش در پروفایل

```text
🧘 قلمرو: امپراتور جاودان — مرحله ۸

⚔️ قدرت رزمی: 125,000,000
⚡ سرعت: 8,400,000
👁 ادراک: 3,200,000

❤️ طول عمر: 3,000,000 سال
⏳ عمر باقیمانده: 2,999,970 سال
🩹 ریکاوری: ×9.4
```

قدرت تجهیزات، سلاح، تبار و سایر سیستم‌ها می‌تواند روی مقدار نهایی جداگانه
اعمال شود تا منبع هر افزایش در پروفایل قابل تشخیص باشد.


# 🖼️ تصاویر رابط کاربری
پنل‌های اصلی ربات اکنون همراه با تصویر نمایش داده می‌شوند.
- `/help` و `/commands`: تصویر راهنمای بازی
- `/profile`: پرتره شخصیت؛ برای جنسیت زن تصویر زن و برای جنسیت مرد تصویر مرد
- `/duel`: تصویر مخصوص دوئل
- پرتره‌ها بر اساس شناسه/نام seed ثابت دارند تا با هر بار اجرا تغییر نکنند.
- زیرساخت `panel_url()` برای اضافه کردن تصویر به بازار، تذهیب، شغل، خدمتکار، فرقه، حکومت، آرنا و سایر پنل‌ها آماده است.


## 🖼️ پنل‌های تصویری بیشتر
راهنما، پروفایل، دوئل، تذهیب، مغازه و فهرست شغل‌ها اکنون تصویر اختصاصی دارند. تصاویر بر اساس seed ثابت ساخته می‌شوند و در پروفایل جنسیت کاربر در انتخاب پرتره اثر دارد.


## 🖼️ تصاویر بخش‌های بازی

برای پنل‌های اصلی تصویر اختصاصی در نظر گرفته شده است:

- 🧑‍🤝‍🧑 خدمتکار → تصویر مخصوص خدمتکار
- 💍 ازدواج → تصویر مراسم/ازدواج
- 🏛️ فرقه → تصویر فرقه
- 👑 حکومت → تصویر قلمرو و پادشاهی
- 🏟️ آرنا → تصویر میدان نبرد
- 🏆 رتبه‌بندی → تصویر رتبه‌بندی
- 🏪 بازار → تصویر بازار
- 🛒 مغازه → تصویر مغازه
- 🧘 تذهیب → تصویر تذهیب
- ⚔️ دوئل → تصویر مبارزه
- 📖 راهنما → تصویر راهنمای بازی
- 💼 شغل → تصویر مشاغل

برای پروفایل و پنل‌هایی که جنسیت مهم است، تصویر می‌تواند بر اساس جنسیت
کاراکتر انتخاب شود؛ مثلاً پروفایل مرد و پروفایل زن تصویر جداگانه دارند.

**نکته فنی:** در نسخه نهایی بهتر است URLهای نمونه با `file_id`های واقعی
تلگرام یا URLهای CDN پایدار جایگزین شوند تا ربات به سرویس تصویر خارجی
وابسته نباشد.


# 🖼️ نسخه تصویری V15

تصاویر اصلی پنل‌ها داخل پوشه `assets/panels` قرار گرفته‌اند و دیگر به
URLهای نمونه وابسته نیستند.

پنل‌های دارای تصویر:
راهنما، پروفایل مرد، پروفایل زن، دوئل، تذهیب، خدمتکار، ازدواج، فرقه،
حکومت، آرنا، رتبه‌بندی، بازار، مغازه، شغل، تبار و تنظیمات.

برای ارسال تصویر در aiogram از `bot.panel_sender.send_panel` استفاده کنید.

## 🗣 ورود بدون کامند

از این نسخه لازم نیست حتماً `/command` بنویسی. می‌توانی اسم بخش را مستقیم تایپ کنی؛ مثلاً «پروفایل»، «تذهیب»، «بازار»، «فرقه»، «آرنا»، «خدمتکارها»، «کاراکترها»، «رتبه‌بندی»، «شغل»، «مأموریت‌ها» یا «راهنما». ربات همان بخش را باز می‌کند.


## آناهیتا — خدمتکار ویژه
- آناهیتا در فهرست **خدمتکارها** قرار دارد، نه کاراکترها.
- قیمت: **۹۹۹ میلیارد سکه**.
- موجودی خرید جهانی: **فقط ۳ عدد در هر ماه** برای همه بازیکنان.
- تصویر اختصاصی آناهیتا همان تصویر ارسالی کاربر است.
- شمارنده خرید ماهانه در `persist_kv` ذخیره می‌شود.

## حفاظت از دیتای بازیکنان در آپدیت
- ربات هیچ `DROP TABLE` یا `TRUNCATE` در راه‌اندازی ندارد.
- مهاجرت دیتابیس فقط ستون/جدول‌های لازم را با `IF NOT EXISTS` اضافه می‌کند.
- PostgreSQL منبع اصلی داده‌های پایدار است و هنگام deploy، داده‌های موجود بازیکنان از DB دوباره بارگذاری می‌شوند.
- برای حفظ اطلاعات در آپدیت، باید همان `DATABASE_URL` قبلی Render را نگه داری و دیتابیس جدید نسازی.

### 💰 پرداخت با ارزهای بالاتر
اگر قیمت یک آیتم به سکه باشد، بازیکن می‌تواند حتی بدون داشتن سکه کافی، با سنگ روحی، بهشتی، آسمانی، خدا و ارزهای بالاتر پرداخت کند؛ سیستم ارزش ارز را محاسبه می‌کند و در صورت نیاز مابه‌التفاوت را به سکه برمی‌گرداند. آیتم‌هایی که `currency` مشخص دارند نیز دقیقاً با همان ارز پرداخت می‌شوند.

### 🏗️ ارتقای ساختمان‌ها
ارتقای خانه، ساختمان تذهیب و ساختمان‌های فرقه باید ابتدا هزینه را با سیستم پرداخت مشترک بررسی کند و سپس سطح را افزایش دهد؛ در صورت کمبود ارز هیچ سطحی ثبت نمی‌شود.


### 💳 پرداخت با ارزهای بالاتر
اگر یک آیتم قیمتش به سکه باشد، داشتن ارزهای بالاتر مثل سنگ خدا، هرج‌ومرج، پوچی، ازلی، تقدیر، جاودان، خلقت یا مطلق برای خرید کافی است و ربات دیگر صرفاً به‌خاطر نداشتن سکه خطا نمی‌دهد. سیستم در صورت نیاز ارز بالاتر را به ردهٔ پایین‌تر می‌شکند و مبلغ دقیق را پرداخت می‌کند.

### 🏗 رفع باگ ارتقا
ارتقای ساختمان تذهیب، خانه و ساختمان‌های فرقه با پرداخت مشترک هماهنگ شده‌اند. اگر پرداخت ناموفق باشد سطح ساختمان تغییر نمی‌کند. برای ساختمان‌های فرقه می‌توان از نام‌های فارسی یا کلیدهای `tower`، `library` و `forge` استفاده کرد.

---
## امکانات V32
⚖️ جرم و تحت تعقیب: `جرم` یا `/crime`  
🗺️ نقشه جهان: `نقشه` یا `/worldmap`  
⚔️ جنگ فرقه‌ای: `/sectwar شناسه`  
🐉 باس جهانی: `باس جهانی` یا `/worldboss`  
🎁 صندوق: `صندوق` یا `/chest`  
📜 مأموریت زنجیره‌ای: `ماموریت زنجیره` یا `/chainmission`  
🌍 رویداد جهانی: `رویداد جهان` یا `/event`  
🤝 اتحاد: `اتحاد` یا `/alliance`  
🏦 بانک: `بانک` یا `/bank`  
⚔️ فرمول قدرت: `/powerformula`
